import asyncio
from claude_agent_sdk import query, ClaudeAgentOptions
import os
import logging

# Configure logging
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

async def main():
    # AWS Bedrock Configuration
    os.environ["CLAUDE_CODE_USE_BEDROCK"] = "1"
    os.environ["AWS_REGION"] = "us-east-1"
    os.environ["ANTHROPIC_MODEL"] = "global.anthropic.claude-sonnet-4-5-20250929-v1:0"
    os.environ["ANTHROPIC_SMALL_FAST_MODEL"] = "us.anthropic.claude-3-5-haiku-20241022-v1:0"
    os.environ["DISABLE_PROMPT_CACHING"] = "1"
    os.environ["AWS_PROFILE"] = "arindam_linux"
    
    # Create test workspace
    test_dir = "./agent_workspace"
    os.makedirs(test_dir, exist_ok=True)
    os.makedirs(f"{test_dir}/src", exist_ok=True)
    os.makedirs(f"{test_dir}/data", exist_ok=True)
    
    # Create sample files for testing
    with open(f"{test_dir}/sample.txt", "w") as f:
        f.write("Line 1: Hello World\nLine 2: Python Programming\nLine 3: Claude Agent SDK")
    
    with open(f"{test_dir}/src/app.py", "w") as f:
        f.write("def greet(name):\n    return f'Hello, {name}!'\n\nprint(greet('World'))")
    
    with open(f"{test_dir}/data/config.json", "w") as f:
        f.write('{"api_key": "test123", "timeout": 30, "debug": true}')
    
    with open(f"{test_dir}/notes.md", "w") as f:
        f.write("# Project Notes\n\n## TODO\n- Task 1\n- Task 2\n\n## Completed\n- Setup")
    
    print("=" * 80)
    print("CLAUDE CODE AGENT - ALL BUILT-IN TOOLS DEMONSTRATION")
    print("=" * 80)
    print("\nBuilt-in Tools: Task, Bash, Glob, Grep, ExitPlanMode, Read, Edit,")
    print("                Write, NotebookEdit, WebFetch, TodoWrite, BashOutput,")
    print("                KillShell, SlashCommand")
    print("=" * 80)
    
    # Example 1: Read Tool - Read file contents
    print("\n### Example 1: Read Tool ###")
    options1 = ClaudeAgentOptions(
        system_prompt="You are a helpful assistant with file reading capabilities.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="Read the contents of sample.txt and tell me how many lines it has.",
        options=options1
    ):
        print(f"[Read] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 2: Write Tool - Create new files
    print("\n### Example 2: Write Tool ###")
    options2 = ClaudeAgentOptions(
        system_prompt="You are a helpful file management assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Write a new file called 'output.txt' with the following content:
        Title: Test Output
        Generated by: Claude Agent
        Timestamp: 2025-10-16
        Status: Success""",
        options=options2
    ):
        print(f"[Write] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 3: Edit Tool - Modify existing files
    print("\n### Example 3: Edit Tool ###")
    options3 = ClaudeAgentOptions(
        system_prompt="You are a code editing assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Edit the src/app.py file:
        1. Add a new function called 'farewell' that says goodbye
        2. Add a docstring to the greet function
        3. Call both functions at the end""",
        options=options3
    ):
        print(f"[Edit] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 4: Bash Tool - Execute shell commands
    print("\n### Example 4: Bash Tool ###")
    options4 = ClaudeAgentOptions(
        system_prompt="You are a system administration assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Use bash commands to:
        1. List all files and directories with details (ls -la)
        2. Show disk usage of the current directory (du -sh)
        3. Display the current working directory (pwd)
        4. Count total number of files (find and wc)""",
        options=options4
    ):
        print(f"[Bash] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 5: Glob Tool - Pattern matching for files
    print("\n### Example 5: Glob Tool ###")
    options5 = ClaudeAgentOptions(
        system_prompt="You are a file search assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Use glob patterns to:
        1. Find all .txt files
        2. Find all .py files in any subdirectory
        3. Find all .json files
        4. List all markdown files (.md)""",
        options=options5
    ):
        print(f"[Glob] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 6: Grep Tool - Search within files
    print("\n### Example 6: Grep Tool ###")
    options6 = ClaudeAgentOptions(
        system_prompt="You are a text search assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Use grep to search for:
        1. Lines containing 'Hello' in sample.txt
        2. Lines containing 'def' in all Python files
        3. Case-insensitive search for 'python' in all .txt files
        4. Lines with 'TODO' in notes.md""",
        options=options6
    ):
        print(f"[Grep] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 7: BashOutput Tool - Capture command output
    print("\n### Example 7: BashOutput Tool ###")
    options7 = ClaudeAgentOptions(
        system_prompt="You are a script execution assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Execute the Python script src/app.py and capture its output.
        Then tell me what the script does and what it printed.""",
        options=options7
    ):
        print(f"[BashOutput] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 8: TodoWrite Tool - Manage TODO lists
    print("\n### Example 8: TodoWrite Tool ###")
    options8 = ClaudeAgentOptions(
        system_prompt="You are a task management assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Create a TODO list file called 'tasks.todo' with the following tasks:
        - [ ] Setup development environment
        - [ ] Write unit tests
        - [ ] Implement feature X
        - [x] Create project structure
        - [ ] Deploy to production""",
        options=options8
    ):
        print(f"[TodoWrite] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 9: WebFetch Tool - Fetch web content
    print("\n### Example 9: WebFetch Tool ###")
    options9 = ClaudeAgentOptions(
        system_prompt="You are a web research assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Fetch the content from https://httpbin.org/json and save it to a file called 
        'web_data.json'. Then read and summarize what data it contains.""",
        options=options9
    ):
        print(f"[WebFetch] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 10: NotebookEdit Tool - Edit Jupyter notebooks
    print("\n### Example 10: NotebookEdit Tool ###")
    options10 = ClaudeAgentOptions(
        system_prompt="You are a Jupyter notebook assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Create a simple Jupyter notebook called 'analysis.ipynb' with:
        1. A markdown cell with title "Data Analysis"
        2. A code cell that imports numpy and pandas
        3. A code cell that creates a simple list and prints it
        4. A markdown cell with "End of Analysis" """,
        options=options10
    ):
        print(f"[NotebookEdit] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 11: Task Tool - Break down complex tasks
    print("\n### Example 11: Task Tool - Complex Workflow ###")
    options11 = ClaudeAgentOptions(
        system_prompt="You are a comprehensive project assistant with all tools available.",
        cwd=test_dir,
        max_iterations=15
    )
    
    async for message in query(
        prompt="""Complete this multi-step task:
        1. Create a Python script called 'data_processor.py' that reads config.json
        2. Add functionality to parse the JSON and print each key-value pair
        3. Execute the script and capture output
        4. Search all files for the word 'debug'
        5. Create a summary report in 'report.txt' with:
           - List of all Python files found
           - Output from data_processor.py
           - Files containing 'debug'
        6. Create a TODO list for next steps""",
        options=options11
    ):
        print(f"[Task] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 12: KillShell Tool - Process management
    print("\n### Example 12: KillShell & Process Management ###")
    options12 = ClaudeAgentOptions(
        system_prompt="You are a process management assistant.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Start a long-running bash command (like 'sleep 100') in the background,
        then demonstrate how to list running processes and kill it if needed.""",
        options=options12
    ):
        print(f"[KillShell] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 13: SlashCommand Tool - Special commands
    print("\n### Example 13: SlashCommand - Meta Operations ###")
    options13 = ClaudeAgentOptions(
        system_prompt="You are an assistant that can use special slash commands.",
        cwd=test_dir
    )
    
    async for message in query(
        prompt="""Use appropriate slash commands or special operations to:
        1. Show help or available commands
        2. Display current configuration
        3. List available tools
        4. Show working directory status""",
        options=options13
    ):
        print(f"[SlashCommand] {message}")
    
    print("\n" + "-" * 80)
    
    # Example 14: Combined Multi-Tool Workflow
    print("\n### Example 14: Complete Project Workflow ###")
    options14 = ClaudeAgentOptions(
        system_prompt="""You are a senior software engineer assistant with full access to all tools.
        You can read, write, edit files, execute commands, search patterns, manage tasks, and more.""",
        cwd=test_dir,
        max_iterations=20
    )
    
    async for message in query(
        prompt="""Create a complete mini-project:
        
        1. Use Glob to find all existing Python files
        2. Use Grep to search for function definitions in those files
        3. Create a new file 'utils.py' with helper functions
        4. Edit an existing Python file to import from utils.py
        5. Use Bash to run all Python files and capture outputs
        6. Fetch some data from https://httpbin.org/uuid and save it
        7. Create a comprehensive TODO list for the project
        8. Generate a final report.md with:
           - Project structure (use Glob results)
           - All function definitions found (from Grep)
           - Execution results (from Bash outputs)
           - Next steps (from TODO list)
        
        Execute this workflow step by step.""",
        options=options14
    ):
        print(f"[Workflow] {message}")
    
    print("\n" + "=" * 80)
    print("ALL TOOL DEMONSTRATIONS COMPLETED!")
    print("=" * 80)
    print(f"\nWorkspace location: {os.path.abspath(test_dir)}")
    print("You can inspect all generated files in the workspace directory.")
    print("\nGenerated files include:")
    print("  - output.txt (Write tool)")
    print("  - tasks.todo (TodoWrite tool)")
    print("  - web_data.json (WebFetch tool)")
    print("  - analysis.ipynb (NotebookEdit tool)")
    print("  - data_processor.py (Task tool)")
    print("  - report.txt & report.md (Combined tools)")
    print("  - utils.py (Complex workflow)")

if __name__ == "__main__":
    asyncio.run(main())